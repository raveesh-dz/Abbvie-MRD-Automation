# PROJECT MEMORY — Query-to-Slide

> Running state of the build so a new session resumes without starting blank.
> Read this first, alongside CLAUDE.md. Update it whenever setup state changes.
> Last updated: 2026-06-04 (user: R / rounaksuranshe@gmail.com)

---

## Environment / gotchas
- **Python:** `python` is a broken Windows Store alias. Use **`py -3`** (Python 3.14.3, pandas 3.0.2, pyyaml installed).
- **Node:** v25.8.1 / npm 11. `pptxgenjs` is installed LOCALLY in `apex-deck-builder/node_modules` (not global) — node resolves it from there, no NODE_PATH needed.
- **LibreOffice:** NOT installed → the deck-builder's visual QA render (pptx→images) can't run on this machine. Verify decks via `deck_config.json` vs `result.csv` instead.
- **Git:** initialised, remote = `github.com/raveesh-dz/Abbvie-MRD-Automation` (origin/main). Earlier MEMORY note said "not initialised" — stale, fixed 2026-06-09. Make a new commit per change to `/semantic/` or `/metadata/`; never `--amend` published commits.
- Schema gate: `py -3 scripts/validate_schema.py` → currently **ALL CLEAR**, both tables active.

## Pipeline output → slides (apex-deck-builder)
- The deck stage is OPT-IN (CLAUDE.md Step 8). Default stop = the 6-file output contract (Step 7). After delivering, ASK "Do you want me to build a slide deck?" and build ONLY on an explicit yes. Never auto-build.
- SKILL.md was rewritten (2026-06-04) to a design-led python-pptx methodology (reference-matched, Lucide icons, integrity sanitizer, self-review). Reference deck: `apex-deck-builder/examples/Deck revamp refrence.pdf` (DataZymes/Otsuka house style — blue section strip, statement title, italic dek, custom diagrams, numbered blocks, TL;DR bar, ▶ DataZymes footer).
- **CURRENT builder: `py -3 scripts/build_deck_pptx.py output/<run_id>`** (python-pptx). Reads `result.csv` + `deck_spec.json` (date_column/title/subtitle/series_columns/total_column), renders a SINGLE content slide (native UC/CD line chart, numbered insights, TL;DR) — the lead/divider slide was REMOVED 2026-06-08 per user (not required). Runs integer-EMU sanitizer + round-trip validation. Tuned to the IBD UC/CD shape.
- Toolchain gaps on this machine: **LibreOffice not installed** → visual self-review render can't run (do structural QA instead). **cairosvg not installed** → Lucide icons substituted with native numbered markers. Montserrat/Roboto likely absent locally (font names still embedded; render substitutes).
- LEGACY path (superseded): `scripts/handoff_deck.py` → `apex-deck-builder/scripts/build_config.js` + `apex_deck.js` (pptxgenjs). Still works for a quick chart-only deck.

## Data (three IQVIA extracts — different grains)
- `data/Weekly_Data_Tabular.csv` — product × week, `TRX_ADJUSTED`. Market = "SI Market + Oral". Range 2024-05-03 → 2026-05-08. No indication breakdown. **SCOPE: this panel is IBD / GI ONLY (user-confirmed 2026-06-04, RULE-301).** A product's weekly volume = its IBD (UC+CD) usage; split weekly totals only across {UC, CD}, never across a product's non-IBD indications. (The Monthly table is national / all 11 indications; it only supplies the UC:CD ratio.)
- `data/Monthly_Data_Tabular.csv` — indication × product × month, `TRX_VOLUME`. 11 indications. Range 2020-05-01 → 2026-04-01.
- `data/sqlqueries_4_gastro_hcp_universe_prod_2_260605_cl.csv` — **NEW (onboarded 2026-06-26). HCP-grain gastro IBD universe.** ~79,806 rows, ~1,040 NPIs. Grain: one row per HCP (`abv_customer_id`) × `product_brand` × `indication_code` (UC/CD) × `data_type` (**TRx OR NBRx**) × `version_flag` (2026-06-05). 442 cols = 35 attrs + 8 rolling windows + **399 `frx*` weekly values** (frx1 = latest = w/e 2026-06-05, −1wk per index, frx399 = 2018-10-19; no date col, positional). Class rollups in `product_brand` {SI_ORAL,TNF,JAK,IL_23,IL_23_IL_12,A4B7,S1P} = Σ constituents → exclude from brand sums. SKYRIZI=OBI. STANDALONE (no cross-table join). HCP grain allowed (above patient level); PII cols present (don't emit raw); pdrp_flag NOT suppressed (user call). ai*_cp_flag meaning UNCONFIRMED, set aside.

## Metadata — DONE
- `metadata/Weekly_Data_Tabular.yaml` — written, passes gate.
- `metadata/Monthly_Data_Tabular.yaml` — written, passes gate.
- `metadata/relationships.yaml` — one allocation edge declared (see RULE-004). No other join is valid: tables differ in grain, metric, and product granularity; PRODUCT is NOT a clean row-join key outside the RULE-003 crosswalk. The HCP universe table is STANDALONE (note added 2026-06-26) — no cross-table edge; only internal slice self-alignment on `abv_customer_id`.
- `metadata/sqlqueries_4_gastro_hcp_universe_prod_2_260605_cl.yaml` — **written 2026-06-26, passes gate (ALL CLEAR, 3 tables active).** All 442 cols enumerated (gate requires exact match). Generated by a scratch script (`gen_hcp_dict.py`); regenerate if a new dated extract arrives (anchor + frx count shift).

## Semantic rules — what exists
Only the indication-allocation rule is defined. Everything else was stripped (was `rx_transactions` placeholder junk).
- **RULE-003 (metrics.md)** — Weekly→Monthly brand crosswalk. Match same FORM, sum dose splits, never fold IV into SQ. Non-trivial maps: RINVOQ=15+30+45MG (exclude bad "180ML"); OMVOH SQ=both SQ dose rows; TREMFYA=SQ 100+200+INDUCTION (SQ only); SKYRIZI=SQ+IV (OBI separate); SIMPONI SC→Simponi SQ; ACTEMRA BIOSIMILARS SQ TOTAL=AVTOZMA SQ+TYENNE SQ. ~26 brands match exactly 1:1.
- **RULE-004 (metrics.md)** — Indication allocation. **REVISED 2026-06-04 (renormalise-within-reported-set).** Split the FULL weekly total across the *reported* indication set: monthly mix for those indications renormalised to sum to 1.0, applied to the weekly total → reported indications sum back to the weekly total. (Superseded the earlier "true share of full mix" approach, which left the total only partially split.) Confirmed by user with example: weekly 100, UC:CD = 40:60 → UC 40, CD 60.
  - KEY CONSEQUENCE: if the reported set is a SUBSET (e.g. IBD = UC+CD only), the ENTIRE weekly total is attributed to that subset — overstates it. So IBD-only and on-label runs give DIFFERENT UC/CD (different denominators). Flag in context.md.
  - Denominator = the reported indication set (renormalised). If PsA included, use PsA (Derm)+PsA (Rheum), EXCLUDE Total PsA. PRODUCT rows only.
  - Week→month by calendar month of `WEEK_ENDING`; weeks past 2026-04 carry forward April 2026 mix. Weeks before the set has any monthly volume show 0. Estimates, not census.
- **Gastro HCP universe rules (added 2026-06-26, reviewed=no — pending user review):**
  - **RULE-005 (metrics.md)** — TRx & NBRx are STORED, selected by `data_type` filter (NOT computed; no arithmetic). Get brand X NBRx in indication Y = filter {product_brand=X, indication_code=Y, data_type='NBRx'} + read frx series. HCP multi-brand view = slice each (brand,indication,data_type) + align on `abv_customer_id` (1:1, verified). Mirrors user's source SQL.
  - **RULE-006 (metrics.md)** — frx* positional weekly series; frx1=most recent (w/e 2026-06-05), each higher index −1 week.
  - **RULE-007 (metrics.md)** — cur_Nwk=Σfrx1..N, pre_Nwk=Σfrx(N+1)..2N for N∈{4,13,26,52} (verified ~1e-8). growth_abs=cur−pre, growth_pct=(cur−pre)/pre (NA when pre=0).
  - **RULE-008 (metrics.md)** — bio/hum decile 0-10 → LOW 0-2 / MED 3-6 / HIGH 7-10 (verified crosstab).
  - **RULE-101 (filters.md)** — exclude class rollups {SI_ORAL,TNF,JAK,IL_23,IL_23_IL_12,A4B7,S1P} from brand sums (= Σ constituents, ratio 1.000; no 2nd molecule-rollup layer).
  - **RULE-302 (context.md)** — gastro_segments (Influencers/Pragmatists/Fast Followers/Traditionalists/Unsegmented).
  - **RULE-303 (context.md)** — SKYRIZI=OBI, SKYRIZI_IV=IV; suffixes _IV/_SQ/_CDV; adalimumab+ustekinumab biosimilar landscape; delimiter dupes (ENTYVIO SQ vs _SQ) → normalise. OPEN: plain ENTYVIO/STELARA = all vs presentation? unresolved.
  - **RULE-304 (context.md)** — hits flags + phy_univ/true_1view/pdrp; ai*_cp_flag SET ASIDE (meaning unconfirmed, ai456=dead).
  - **RULE-401 (time.md)** — frx anchor + date map; default window = cur_13wk when none stated. Latest week (frx1, w/e 2026-06-05) is COMPLETE, not provisional (user-confirmed 2026-06-26).
- `_rule_log.csv` — RULE-003, RULE-004, RULE-301 (reviewed=yes); RULE-005..008, 101, 302..304, 401 (reviewed=**no**).

## Runs completed
- run_2026-06-04_001 — DELETED 2026-06-04. Was an on-label (Ps/PsA/UC/CD) split; invalid once the weekly panel was confirmed IBD-only (no psoriasis volume in an IBD panel). Superseded by run_002.

- **run_2026-06-04_002** — "weekly tremfya TRx split by indications for IBD market". The CURRENT valid deliverable. IBD = {UC, CD}; weekly panel is IBD-only (RULE-301) so the full weekly total splits across UC/CD via the national monthly UC:CD ratio (RULE-004 renormalise-within-set). Clean function-based commented code. UC+CD = weekly total; UC ~3,121 / CD ~3,905 on 2026-05-08 (CD overtook UC during 2025). PASS, 106 weeks, columns reconcile to total. Standalone (base_plan: none). DECK built: `deck.pptx` (divider + UC/CD/IBD chart slide + latest-vs-4wk table) via `scripts/handoff_deck.py` + `deck_spec.json`.
- **run_2026-06-04_003** — fresh timestamped re-run of the same IBD query (user wanted a new run, not a reuse of run_002). Identical method/result (UC ~3,121 / CD ~3,905 on 2026-05-08). PASS, 106 weeks. DECK built via the NEW python-pptx builder (`scripts/build_deck_pptx.py`) in the DataZymes reference style: divider + content slide (UC/CD line chart, numbered insights, "Crohn's overtook UC ~Oct 2025", TL;DR). Sanitizer + round-trip validation PASS; visual render not done (no LibreOffice).
- **run_2026-06-08_001** — same weekly TREMFYA IBD UC/CD split, new standalone run (fresh session, full boot sequence re-run; schema gate ALL CLEAR). Identical established method (RULE-003 + RULE-004 renormalise-within-set, RULE-301 IBD-only). Result identical: UC 3,121 / CD 3,905 / total 7,026 on 2026-05-08; durable CD>UC crossover from 2025-10-03. PASS, 106 weeks, 0 nulls, UC+CD reconciles to total (diff ~9e-13). 6-file contract delivered. DECK built (python-pptx `scripts/build_deck_pptx.py`, deck_spec.json same shape as run_003): 2 slides (divider + UC/CD line-chart content slide, statement title "Crohn's Has Overtaken Ulcerative Colitis in Tremfya's IBD Mix", numbered insights, crossover ~Oct 2025). Structural QA PASS — chart series trace to result.csv (UC last 3120.92 / CD last 3905.16), integrity 0 issues, round-trip OK. Visual render not run (no LibreOffice); cairosvg absent so Lucide icons = native markers.
- **run_2026-06-08_002** — "skyrizi TRx split by indication for IBD market". KEY FINDING: the plain weekly `SKYRIZI` line is only ~11 TRx/wk; **`SKYRIZI OBI` (on-body injector, the IBD maintenance presentation) is ~99.9% of Skyrizi's IBD volume (~15,645/wk).** User first said exclude OBI, then (after the magnitude was surfaced) confirmed **SKYRIZI + OBI combined**. Method: both weekly lines summed; monthly mix = SKYRIZI SQ+IV+OBI renormalised within {UC,CD} (RULE-004), RULE-301 IBD-only. Result: CD dominates 100% of weeks (no crossover); latest 2026-05-08 total 15,656 → UC 3,622 (23.1%) / CD 12,035 (76.9%). UC share grew ~3.5%→23% since its 2024 UC launch; total ~4.3× over 106 wks. PASS, 0 nulls, reconciles to total. 6-file contract delivered. DECK built (python-pptx): divider + content slide (UC/CD line chart, statement title "Crohn's Dominates Skyrizi's IBD Mix — but Ulcerative Colitis Is Climbing", 3 numbered insights, TL;DR). Structural QA PASS — chart series trace to result.csv (UC 3621.58 / CD 12034.60), no mojibake, integrity sanitizer + round-trip OK. Visual render not run (no LibreOffice).
  - NOTE for future Skyrizi/OBI work: a brand's "OBI" weekly line can be the dominant volume — always check OBI magnitude before excluding it. (Combined-entity mix UC 3,622/CD 12,035 differs slightly from per-line-then-summed UC ~3,309/CD ~12,347; both agree CD ~77–79%.)
  - BUILDER CHANGE (`scripts/build_deck_pptx.py`): now SPEC-DRIVEN — narrative pulled from deck_spec.json (`eyebrow`, `section_strip`, `statement_title`, `dek_template`, `chart_caption`, `blocks` [num/head/body_template], `tldr`); templates `.format()` over a computed ctx (first_total, latest_total, ramp, first_month, cross_month, latest_uc/cd, latest_label, first_uc_pct, latest_uc_pct, latest_cd_pct). Tremfya literals kept as fallback DEFAULTS so the old specs (run_003, run_0608_001) render identically (verified). ALSO fixed: spec + result.csv now read with `encoding="utf-8"` (Windows cp1252 default was mojibaking `·`/`—`). Use these spec fields for any non-Tremfya brand so the deck doesn't inherit the false "crossover/overtaken" story.

- **run_2026-06-10_001** — inbox q_...001: "Monthly Humira TRx by indication, latest 6 months" (Monthly table only, all 10 reported indications, Dec 2025–May 2026). build_deck=false. PASS. Headline: RA/JIA largest at 49,379 May 2026, ~2.1× Crohn's, after uniform ~30% step-down Dec→Jan.
- **run_2026-06-10_002** — inbox q_...002: "Current week vs previous week growth by Humira by indication". build_deck=true. Weekly HUMIRA (1:1 monthly match, RULE-003), IBD-only (RULE-301), split UC/CD via RULE-004 renormalise-within-set. Data REFRESHED: weekly now → 2026-07-03, monthly → 2026-06-01. Latest 2 wks: prev 2026-06-26 / curr 2026-07-03. Result: IBD total 8,963 → 9,142 (+2.0%); CD +120 (5,983→6,103), UC +60 (2,980→3,040). KEY ARTIFACT: both weeks resolve to the SAME June 2026 mix (06-26 by month, 07-03 by carry-forward past monthly max) → UC and CD grow at IDENTICAL % (+2.0); only absolute differs. Flagged in context+takeaways. Also flagged: smooth-rising weekly tail likely projected (no provisional rule, MEMORY pending #3). PASS, 3 rows, reconciles. Registered views.yaml id q_...002 (kind indication_split, deck:true). DECK built — see NEW builder below.
  - **NEW BUILDER: `scripts/build_deck_wow.py`** — sibling to build_deck_pptx.py for the COMPARISON shape (result.csv = one row per indication with prev_week_<date>/current_week_<date>/wow_abs/wow_pct cols), which the line builder cannot render. Reuses brand tokens, helpers, footer, sanitizer+validate from build_deck_pptx via import (line builder UNTOUCHED → registered line decks unaffected). Renders a grouped COLUMN chart (prev vs current wk per indication) instead of a line; narrative templates in deck_spec.json (`chart_kind:"wow_compare"`, statement_title/dek_template/blocks/tldr) .format()'d over a ctx computed from result.csv (prev/curr per UC/CD/total, abs, total_pct, cd/uc pct). Numbers read at build time, none hard-coded. Structural QA PASS (chart traces to result.csv, em-dash/arrow/middot clean, no U+FFFD, integrity 0 issues, round-trip OK). Visual render not run (no LibreOffice). Usage: `py -3 scripts/build_deck_wow.py output/<run_id>`.

## DATA PERTURBED 2026-06-10 — CURRENT = ±25% (synthetic). Old numbers SUPERSEDED
- **What:** every TRx value × per-cell random factor via **`scripts/perturb_data.py --low 0.75 --high 1.25`** (±25%, `--seed 20260610`, reproducible, never negative, zeros stay 0). Earlier ran at ±7%; user then asked for ±25%. **Re-perturb is done from the TRUE ORIGINALS (`_pre_perturb_backup/`), NOT compounded** on the prior ±7% — restore originals → perturb. ALL hardcoded result figures elsewhere in this MEMORY are STALE; the live `result.csv` files are the source of truth.
- **Backups / reversibility:** true originals at **`./_pre_perturb_backup/`** (outside `data/`, dashboard-unreachable). To revert or re-perturb at a new %: copy `_pre_perturb_backup/*` → `data/*.csv`, then run perturb_data.py with the new `--low/--high`.
- **Aggregates:** WEEKLY rollups recomputed from perturbed leaves (`MARKET_TOTAL` = Σ flag-N PRODUCT; `Total Non-Approved Volume` = Σ flag-Y; all 114 weeks pass the identity guard, diff 0). MONTHLY rollups NOT recomputed (undeclared hierarchy; `MARKET_TOTAL` ≠ Σ products) — monthly internal totals do NOT reconcile. Harmless: analyses read monthly only at PRODUCT-grain UC/CD.
- **ACTIVE RUNS = 7** (Humira IBD WoW DELETED — see below). Re-run cascade done: reran `analysis_code.py`, `validate_result.py` PASS on all 7, regenerated `context.md`+`takeaways.md` (numbers + crossover + direction derived from data, **NO perturbation caveat** per user), rebuilt 6 line decks (run_2026-06-10_001 has none), refreshed `run_meta.json` (**no `data_state` field** — perturbation marker kept off the dashboard). Schema gate ALL CLEAR.
- **Weekly panel = 114 weeks** (latest 2026-07-03; monthly latest 2026-06-01). The 6 weekly-series plans carry `expected_row_count: 114` and 2026-07-03 anchors.
- **Current ±25% result snapshots:**
  - Tremfya (run_0604_002/_003, run_0608_001, tremfya_ibd_split — identical): latest 2026-07-03 UC **3,770** / CD **4,711** / total **8,481** (CD leads). **Durable crossover MOVED to ~2026-03-06** (was Oct 2025 at ±7%) — insights now DERIVE the crossover week from data, never hardcode it.
  - Skyrizi (run_0608_002, skyrizi_ibd_split): latest UC **3,555** (23%) / CD **12,157** (77%) / total **15,712**. CD leads all 114 wks; UC climbed ~2%→23%. Ramp ~3.7×.
  - Humira monthly (run_2026-06-10_001): Jan–Jun 2026; Jun 2026 RA/JIA **47,889** (largest, ~1.8× CD **27,109**). Insight derives top indication + ratio dynamically.
- **Humira IBD WoW DELETED (user, 2026-06-10):** removed `output/run_2026-06-10_002/` folder, its `views.yaml` entry (`q_20260610_154509916817_002`), the engine_inbox item `q_20260610_154509916817_002.json`, AND `scripts/build_deck_wow.py` (the wow-compare builder existed only for it). Gone from pipeline, folder, dashboard. `build_deck_pptx.py` (line builder) untouched.

## Humira monthly deck + NEW monthly builder + server builder-dispatch (2026-06-10)
- **`scripts/build_deck_monthly.py`** — sibling builder for the MONTHLY multi-indication shape (result = month rows × indication cols), which the UC/CD line builder can't render. Multi-line chart of top-N indications (default 5, `top_n` in spec), spec-driven narrative (`chart_kind: "monthly_multiline"`, statement_title/dek/blocks/tldr templated over a ctx computed from result.csv: top/second/third indication + values, ratio, latest_month, cd_val/uc_val, n_months). Reuses brand tokens + sanitizer + validate from build_deck_pptx (UC/CD builder untouched). Usage: `py -3 scripts/build_deck_monthly.py output/<run_id>`.
- **run_2026-06-10_001 now has a downloadable deck:** wrote `deck_spec.json` (monthly_multiline), built `deck.pptx`, set `deck_available: true` in run_meta and `deck: true` on its `views.yaml` entry → served by `/api/views/{id}/deck`.
- **server/views.py `run_view_core` now DISPATCHES the deck builder by `deck_spec.chart_kind`** — `monthly_multiline` → build_deck_monthly.py, else build_deck_pptx.py. Prevents a dashboard re-run from mis-rendering the monthly deck with the UC/CD line builder.
- **LIVE DATA MOVED via dashboard simulate (user, mid-session):** weekly now max **2026-07-31** (118 wks), monthly **2026-07-01** (75 mo) — simulate clones latest period ×1.02. Pipeline reran all result.csv + rebuilt the 6 line decks. run_001 resynced fully (RA/JIA Jun→Jul, now **48,847**, ~1.8× CD, window Feb–Jul 2026). ⚠️ The OTHER 6 runs' result.csv + decks are current, but their **`takeaways.md`/`context.md` text is stale** (still says 2026-07-03 / pre-simulate ±25% numbers) — regenerate from current result.csv when the demo data settles.

## DASHBOARD ↔ data: only PERTURBED data reachable + NO perturbation caveat (user, 2026-06-10)
Server (`server/app.py`) mounts only `web/` statically; `data/` + `output/` reachable ONLY via `/api`. After each (re-)perturb, re-sever:
1. **`/api/data/reset`** restores from `data/_original/` → overwrite `_original/` with the perturbed live CSVs (verified `_original` == live). Reset yields perturbed data.
2. **`/api/views/{id}/diff`** compares `result.csv` vs `result_prev.csv` → set each run's `result_prev.csv` = its perturbed `result.csv` (no pre-perturb deltas).
3. `data/.snapshot.json` acknowledged to perturbed live.
4. **Caveat removal:** dashboard surfaces `takeaways.md` (via `/api/views/{id}/result`) and `run_meta.json` — neither mentions perturbation now (regen drops the caveat; `data_state` dropped). Verified `grep -i perturb` over takeaways/run_meta = clean. `context.md` clean; decks never mentioned it.

## STILL PENDING (user will add later — do NOT invent these)
Foundational rules still undefined; engine should ask before assuming:
1. **Market share denominator** — MARKET_TOTAL row vs sum of PRODUCT rows? Within-indication for Monthly?
2. **MARKET_TOTAL / Total Non-Approved Volume** — default double-count handling (dictionaries flag both as traps).
3. **Provisional periods** — does source treat latest week(s)/month as incomplete? (tails look stable; unconfirmed.)
4. **Time defaults** — no window stated → Weekly = ? , Monthly = ?
5. **Context patterns** for takeaways — biosimilar erosion (HUMIRA/STELARA), Q4 deductible spike, IV→SQ shift. `context.md` is empty.
6. **TRx definition rule** — note: the Weekly/Monthly tables have NO NRx (adjusted/projected volumes, not script counts). **CORRECTION (2026-06-26): the NEW gastro HCP universe table DOES carry NBRx** (data_type=NBRx), per RULE-005. The "no NRx" note applies only to Weekly/Monthly.
7. **ai*_cp_flag meaning** (gastro HCP table) — set aside 2026-06-26; numeric-code semantics (ai4/5/6/45/10) unconfirmed; ai456 is dead. Revisit with user.
8. **Plain ENTYVIO / STELARA** (gastro HCP table) — whether they're all-presentation totals vs a specific presentation is unresolved (RULE-303). Verify before treating as a brand total.

## Cleanup status — rx_transactions (the old dummy table)
- Live config (`metadata/`, `semantic/`): **clean**, no references.
- The old `examples/` demo was **archived to `_reference/examples/`** (not deleted — git isn't set up).
- CLAUDE.md now instructs the engine to **SKIP `/_reference/`** entirely (read/validate/analyze/cite none of it).
- `PLAN.md` / `USER_GUIDE.md` still mention `rx_transactions` as illustrative doc text — left as-is (not config; user did not ask to scrub docs).
